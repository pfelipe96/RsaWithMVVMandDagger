<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.paulo.rsawithmvvm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".mainActivity.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeWithoutActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".criptografar.CriptografiaActivity"
            android:label="@string/action_bar_title_encryption"
            android:parentActivityName=".mainActivity.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeWithoutActionBar">
        </activity>

        <activity
            android:name=".descriptografar.DescriptografiaActivty"
            android:label="@string/action_bar_title_decryption"
            android:parentActivityName=".mainActivity.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeWithoutActionBar">
        </activity>
    </application>

</manifest>